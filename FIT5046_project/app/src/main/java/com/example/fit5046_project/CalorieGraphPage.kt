package com.example.fit5046_project

/*@Composable
fun CalorieGraphPage(
    navController: NavController,
    caloriesConsumed: Int,     // change it back to list List<Int> so that it can hold list of ints
    caloriesBurned: Int
) {
    Text(
        text = "Calorie Graph Page",
        style = MaterialTheme.typography.headlineLarge,
        modifier = Modifier.padding(16.dp)

    )

    Text(
        text = "Calories burned: ",
        style = MaterialTheme.typography.headlineSmall,
        modifier = Modifier
            .fillMaxWidth()
            .padding(vertical = 8.dp)

    )
    Button(
        onClick = {
            // Navigate back to the previous page when the button is clicked
            navController.popBackStack()
        },
        modifier = Modifier
            .padding(16.dp)
            .fillMaxWidth()
    ) {
        Text(text = "Go Back")
    }
}

@Preview(showBackground = true)
@Composable
fun CalorieGraphPreview() {
    FIT5046_projectTheme {
        val navController = rememberNavController()
        CalorieGraphPage(navController = navController,23,45)
    }
}

 */



